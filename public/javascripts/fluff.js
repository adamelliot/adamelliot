/**
 * All the fancy stuff that looks cool, but really is just what I call
 * JavaScript vanity. It also can take up a bit of space, and get a bit messy
 * at times. I keep it out of the part of the application that needs to be
 * kept clean and running well.
 */

window.AdamElliot = window.AdamElliot || {};

AdamElliot.Fluff = (function() {
   
  var Klass = function () {
  };
  
  return Klass;
})();