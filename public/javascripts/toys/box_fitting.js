AdamElliot.Toys.BoxFitting = (function() {
  source = 
  
  var Klass = function(frame, fps) {
    AdamElliot.Toy.call(this, frame);

    

  };
  Klass.prototype = AdamElliot.Toy;
  
  return Klass;
}

